import { ListCustomersPipe } from './list-customers.pipe';

describe('ListCustomersPipe', () => {
  it('create an instance', () => {
    const pipe = new ListCustomersPipe();
    expect(pipe).toBeTruthy();
  });
});
