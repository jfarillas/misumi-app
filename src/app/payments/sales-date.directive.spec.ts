import { SalesDateDirective } from './sales-date.directive';

describe('SalesDateDirective', () => {
  it('should create an instance', () => {
    const directive = new SalesDateDirective();
    expect(directive).toBeTruthy();
  });
});
